import time
print(time.time()) # prints the current time in seconds since epoch


print(time.sleep(5))    # sleeps for 5 seconds
print(time.time()) # prints the current time in seconds since epoch after sleep