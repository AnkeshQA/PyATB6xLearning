Object oriented Programming in Python

Object Oriented Programming (OOP) is a programming paradigm that uses classes and objects 

Class is user defined data type which defined data type which means every class act as a bluprint, which will basically contain attributes and behaviour

Attributes is some which you usually have and behvaiour is something how u behave or what you do afterwards


Person (class)
    - Attributes: name, age, gender, occupation 
    - Behaviour: walk(), talk(), eat(), sleep()
behaviour are functionality , methods, functions

Whenever we say data member , properties ,--> attributes all are same
whenever anyone says member function , functionality, --> behaviour all are same 

when class is created no memory is allocated, when object is created memory is allocated


When object is created out of the class for example  

Dog class 
dog is an empty class which contain attributes and behaviour
but when we create real entity out of it for example 
we created 3 dog class 
Dog 1 
breed : sheperd
size : large
age : 5 years
color : black 

Dog 2
breed : buldog
size : medium
age : 3 years
color : brown

Dog 3
breed : labrador
size : small
age : 4 years
color : yellow

here we created 3 objects out of dog class
object is actually called as Instance of a class 

These are object or instance of the Dog class 


Object is runtime entity because it comes at runtime whenever we start creating an object.

- **Object** is a run-time entity. It is an instance of the class. An object can represent a person, place or any other item.
- **Instance of the Class, Real world entity from the class.**
- **Object** is an instance of a class. 
- All data members and member functions of the class can be accessed with the help of objects.

